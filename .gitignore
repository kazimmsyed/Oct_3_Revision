.DS_Store
# Do not .gitignore in .gitignore file. Since it needs to be updated to tell github what to ignore everytime.
# Also, if u intent to ignore a repo but not certain files, keep the certain files like ignore everything in the dir except t
#file
